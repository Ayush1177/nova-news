generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model Post {
  id        String   @id @default(cuid())
  author    String
  title     String?
  content   String
  createdAt DateTime @default(now())

  @@index([createdAt])
}